import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { PhoneCallIcon, MapPinIcon, HospitalIcon, ShieldAlertIcon } from 'lucide-react';

export function Emergency() {
  const [confirm, setConfirm] = useState(false); const [coords, setCoords] = useState<GeolocationCoordinates | null>(null); const [locationError, setLocationError] = useState('');
  const shareLocation = () => { setLocationError(''); if (!navigator.geolocation) { setLocationError('Location detection is unavailable in this browser.'); return; } navigator.geolocation.getCurrentPosition((position) => setCoords(position.coords), () => setLocationError('Location permission was denied. You can still use the other assistance options.')); };
  const locationText = coords ? `${coords.latitude.toFixed(6)}, ${coords.longitude.toFixed(6)}` : '';
  return <div className="mx-auto max-w-[900px] px-5 py-10"><header className="border border-crit/20 bg-white rounded-sm p-6"><div className="flex gap-4 items-start"><ShieldAlertIcon className="w-7 h-7 text-crit shrink-0" /><div><p className="text-[11px] font-semibold tracking-[0.14em] text-crit">SAFETY SUPPORT</p><h1 className="mt-2 text-3xl font-semibold text-navy tracking-tight">Emergency Assistance</h1><p className="mt-2 text-sm text-charcoal-600">If you are in immediate danger, contact the appropriate emergency service.</p></div></div></header><div className="mt-6 grid sm:grid-cols-2 gap-px bg-line border border-line rounded-sm overflow-hidden"><section className="bg-white p-5"><p className="text-[11px] font-semibold tracking-wide text-charcoal-400">IMMEDIATE DANGER</p><h2 className="mt-2 text-lg font-semibold text-navy">Contact emergency services</h2><p className="mt-2 text-sm text-charcoal-600">India's configured emergency number is 112. Confirm before any call is initiated.</p>{confirm ? <div className="mt-4 border border-crit/20 bg-crit-soft p-4"><p className="text-sm font-semibold text-navy">Are you sure you want to initiate an emergency call?</p><div className="mt-3 flex gap-2"><button onClick={() => setConfirm(false)} className="border border-line bg-white px-3 py-2 text-xs font-semibold">CANCEL</button><a href="tel:112" className="bg-crit text-white px-3 py-2 text-xs font-semibold">CONTINUE · 112</a></div></div> : <button onClick={() => setConfirm(true)} className="mt-4 inline-flex items-center gap-2 bg-crit text-white px-4 py-3 rounded-sm text-xs font-semibold"><PhoneCallIcon className="w-4 h-4" /> EMERGENCY CALL</button>}</section><section className="bg-white p-5"><p className="text-[11px] font-semibold tracking-wide text-charcoal-400">TOURIST ASSISTANCE</p><h2 className="mt-2 text-lg font-semibold text-navy">Connect to available help</h2><p className="mt-2 text-sm text-charcoal-600">No live tourism assistance integration is configured in this prototype.</p><button disabled className="mt-4 border border-line text-charcoal-400 px-4 py-3 rounded-sm text-xs font-semibold">TOURIST ASSISTANCE UNAVAILABLE</button></section><section className="bg-white p-5"><p className="text-[11px] font-semibold tracking-wide text-charcoal-400">SHARE LOCATION</p><h2 className="mt-2 text-lg font-semibold text-navy">Share my location</h2><p className="mt-2 text-sm text-charcoal-600">YatraShield needs your permission to access your current location. It is not stored.</p><button onClick={shareLocation} className="mt-4 inline-flex items-center gap-2 border border-navy/20 text-navy px-4 py-3 rounded-sm text-xs font-semibold"><MapPinIcon className="w-4 h-4" /> {coords ? 'LOCATION DETECTED' : 'SHARE MY LOCATION'}</button>{coords && <div className="mt-3 border-t border-line pt-3 text-sm text-charcoal-600"><p className="font-mono tabular">{locationText}</p><button onClick={() => void navigator.clipboard?.writeText(locationText)} className="mt-2 text-xs font-semibold text-navy-500">COPY LOCATION</button></div>}{locationError && <p role="alert" className="mt-3 text-xs text-crit">{locationError}</p>}</section><section className="bg-white p-5"><p className="text-[11px] font-semibold tracking-wide text-charcoal-400">MEDICAL</p><h2 className="mt-2 text-lg font-semibold text-navy">Find nearby hospital</h2><p className="mt-2 text-sm text-charcoal-600">Open a map search for medical assistance near your current position.</p><a target="_blank" rel="noreferrer" href="https://www.google.com/maps/search/hospital" className="mt-4 inline-flex items-center gap-2 border border-navy/20 text-navy px-4 py-3 rounded-sm text-xs font-semibold"><HospitalIcon className="w-4 h-4" /> FIND NEAREST HOSPITAL</a></section></div><section className="mt-6 border border-line bg-[#f4f7fa] rounded-sm p-5"><h2 className="text-sm font-semibold text-navy">Transport or payment concern?</h2><p className="mt-2 text-sm text-charcoal-600">Review your recent risk result and preserve booking or payment evidence. Emergency assistance does not automatically send your trip context anywhere.</p><Link to="/risk-check" className="mt-4 inline-block text-xs font-semibold text-navy-500">REVIEW A RISK CHECK</Link></section><p className="mt-4 text-[11px] text-charcoal-400">Demo / prototype mode. YatraShield does not operate a call centre or emergency response service.</p></div>;
}
